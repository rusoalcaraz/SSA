# -------------------------------------------------------
# Frontend — variables de entorno
# Copia este archivo como .env.local
# -------------------------------------------------------

# URL base de la API (el proxy de Vite la redirige en desarrollo, no es necesaria en dev)
# En produccion apunta al dominio real del backend.
VITE_API_URL=http://localhost:4000/api/v1
